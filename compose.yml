services:
  webfeesh:
    build:
      context: .
      dockerfile: ./Containerfile
      target: final
    image: webfeesh
    container_name: webfeesh
    environment:
      DB_PATH: /app/data/app.db
    ports:
      - 8080:4000
    volumes:
      - ./data:/app/data
