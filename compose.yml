services:
  webfeesh-backend:
    build:
      context: .
      dockerfile: ./Containerfile
      target: final
    container_name: webfeesh-backend
    environment:
      DB_PATH: /app/data/app.db
    ports:
      - 8080:4000
    volumes:
      - ./test:/app/data
